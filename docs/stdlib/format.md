# `std.format`

`std.format` converts primitive values to owned Strings.

```foundation
fn I32(value i32) String
fn U64(value u64) String
```

Integer output is decimal, locale-independent, and covers the complete primitive range.
