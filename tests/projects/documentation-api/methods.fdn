package docs.fixture

methods User {
    // Prints the visible name.
    @Operation("read")
    fn Display(self) void {
        print(self.Name)
    }

    fn scoreValue(self) i32 {
        self.score
    }
}
