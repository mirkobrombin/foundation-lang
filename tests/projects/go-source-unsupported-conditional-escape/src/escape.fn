package sample.conditional_escape

fn twice(value i32) i32 {
    value * 2
}

fn EscapeFromArgument(value Option<i32>) i32 {
    twice(match value {
        None: {
            return 0
        }
        Some(inner): inner
    })
}
