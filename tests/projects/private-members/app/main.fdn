package members.app

import members.lib

fn main() i32 {
    const value = lib.Make()
    const state = lib.State()
    const code = match state {
        hidden: 0
        Visible: 1
    }
    value.hidden + code
}
