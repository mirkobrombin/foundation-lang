fn twice(value usize) usize {
    value * 2
}

fn main() i32 {
    const values = [10, 20, 30]
    const index usize = 1
    if values[index] != 20 return 1
    if len(values) != 3 return 2
    if twice(index + 2) != 6 return 3
    0
}
