package example.replace_order

fn placeIndex() usize {
    panic("place evaluated")
}

fn replacement() String {
    panic("replacement failed")
}

fn main() i32 {
    var values = ["old"]
    const previous = replace values[placeIndex()] with replacement()
    discard previous
    0
}
