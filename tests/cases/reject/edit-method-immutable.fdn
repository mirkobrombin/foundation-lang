struct Counter {
    value i32

    fn add(&self, amount i32) void {
        self.value = self.value + amount
    }
}

fn main() i32 {
    const counter = Counter { value = 0 }
    counter.add(1)
    0
}
