import foundation.di

service Left {
    ctor New(right Right) {
        discard right
        Left {}
    }
}

service Right {
    ctor New(left Left) {
        discard left
        Right {}
    }
}

fn main() i32 {
    0
}
