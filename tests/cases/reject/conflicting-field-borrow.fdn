struct Pair {
    left i32
    right i32
}

fn inspect(left view i32, right edit i32) void {
    discard left
    discard right
}

fn main() i32 {
    var pair = Pair { left = 1 right = 2 }
    inspect(view pair.left, edit pair.right)
    0
}
