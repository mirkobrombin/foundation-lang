enum Holder {
    Empty
    Callback(fn() String)
}

fn main() i32 {
    const value = "borrowed"
    const holder = Holder.Callback(fn() String capture value {
        value
    })
    discard holder
    0
}
