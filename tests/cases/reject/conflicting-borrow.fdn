struct User {
    id String
}

fn compare(left User, &right User) void {
    print(left.id)
    print(right.id)
}

fn main() i32 {
    var user = new User { id = "seven" }
    compare(user, &user)
    0
}
