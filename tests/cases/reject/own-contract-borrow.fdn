contract Consumer {
    fn take($self) i32
}

struct Value implements Consumer {
    value i32

    fn take($self) i32 {
        self.value
    }
}

fn consume(value Consumer) i32 {
    value.take()
}

fn main() i32 {
    const value = Value { value = 1 }
    consume(value)
}
