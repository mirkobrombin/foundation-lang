contract Consumer {
    fn take($self) i32
}

struct Inner implements Consumer {
    value i32

    fn take($self) i32 { self.value }
}

struct Invalid implements Consumer {
    inner Inner

    delegate inner as Consumer
}

fn main() i32 { 0 }
