import foundation.actions
import foundation.di

@di.Scope(.Singleton)
service Session {
    ctor New() {
        Session {}
    }

    @actions.Name("session.close")
    action Close($self) void {
        discard self
    }
}

fn main() i32 {
    0
}
