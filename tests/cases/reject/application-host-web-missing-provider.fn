package test.web.reject

import foundation.web

struct Store {}

@web.Route(.GET, "/items")
fn GetItems(@web.Inject() store Store) web.Response {
    web.Text(200, "items")
}

fn main() i32 {
    0
}
