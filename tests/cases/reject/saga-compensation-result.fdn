fn begin(value i32) Result<void, bool> {
    .Ok
}

fn undo(value i32) Result<i32, bool> {
    .Ok(value)
}

fn finish(value i32) Result<i32, bool> {
    .Ok(value)
}

saga Broken(input i32) Result<i32, bool> {
    step begin using begin
        compensate undo
    step finish using finish
}

fn main() i32 {
    0
}
