package test.host

import foundation.di

@di.Scope(.Scoped)
service Session {
    ctor New() {
        Session {}
    }
}

@di.Scope(.Singleton)
service Server {
    ctor New(session Session) {
        discard session
        Server {}
    }
}

fn main() i32 {
    0
}
