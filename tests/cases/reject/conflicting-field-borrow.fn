struct Pair {
    left String
    right String
}

fn inspect(left String, &right String) void {
    discard left
    discard right
}

fn main() i32 {
    var pair = Pair { left = "left" right = "right" }
    inspect(pair.left, &pair.right)
    0
}
