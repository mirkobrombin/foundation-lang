package test.host

import foundation.di

@di.Scope(.Singleton)
service Source {
    ctor New() {
        Source {}
    }
}

@di.Scope(.Singleton)
service Sink {
    ctor New($source Source) {
        discard source
        Sink {}
    }
}

fn main() i32 {
    0
}
