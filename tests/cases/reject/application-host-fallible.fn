package test.host

import foundation.di

@di.Scope(.Singleton)
service Worker {
    ctor New() Result<bool> {
        .Ok(Worker {})
    }
}

@di.Scope(.Singleton)
service Logger {
    ctor New() Result<String> {
        .Ok(Logger {})
    }
}

fn main() i32 {
    0
}
