struct User {
    id String
}

fn keep(left User, $right User) void {
    print(left.id)
    print(right.id)
}

fn main() i32 {
    const user = new User { id = "seven" }
    keep(user, $user)
    0
}
