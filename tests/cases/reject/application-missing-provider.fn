import foundation.di

contract Database {
    fn Ready(self) bool
}

@di.Scope(.Singleton)
service Api {
    ctor New(database Database) {
        discard database
        Api {}
    }
}

fn main() i32 {
    0
}
