import foundation.di

@di.Scope(.Transient)
service RequestState {
    ctor New() {
        RequestState {}
    }
}

@di.Scope(.Singleton)
service Application {
    state RequestState

    ctor New($state RequestState) {
        Application { state = state }
    }
}

fn main() i32 {
    0
}
