package test.host

import foundation.actions
import foundation.di

@di.Scope(.Singleton)
service Runner {
    ctor New() {
        Runner {}
    }

    @actions.Name("run")
    @actions.Policy("")
    action Run(self) i32 {
        0
    }
}

fn main() i32 {
    0
}
