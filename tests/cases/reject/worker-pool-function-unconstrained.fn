fn observe<T>(value T) void {}

fn hold<T>($value T) transferable fn() void {
    fn() void capture($value) {
        observe(value)
    }
}

fn main() i32 {
    const operation = hold(7)
    operation()
    0
}
