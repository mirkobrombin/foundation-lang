struct User {
    id i32
}

fn consume($user own User) bool {
    true
}

fn main() i32 {
    const user = own User { id = 7 }
    const consumed = false && consume(user)
    if consumed {
        return 1
    }
    user.id
}
