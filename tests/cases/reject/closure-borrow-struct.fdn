struct Holder {
    callback fn() String
}

fn main() i32 {
    const value = "borrowed"
    const holder = Holder {
        callback = fn() String capture value {
            value
        }
    }
    discard holder
    0
}
