package test.host

import foundation.di

@di.Scope(.Scoped)
service First {
    ctor New() Result<bool> {
        .Ok(First {})
    }
}

@di.Scope(.Scoped)
service Second {
    ctor New() Result<String> {
        .Ok(Second {})
    }
}

fn main() i32 {
    0
}
