package test.host

import foundation.actions
import foundation.di

@di.Scope(.Singleton)
service Calculator {
    ctor New() {
        Calculator {}
    }

    @actions.Name("sum")
    action Sum(self, values [i32]) i32 {
        values[0]
    }
}

fn main() i32 {
    0
}
