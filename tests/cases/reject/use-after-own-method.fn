struct Value {
    number i32

    fn take($self) i32 {
        self.number
    }
}

fn main() i32 {
    const value = own Value { number = 42 }
    const number = value.take()
    number + value.number
}
