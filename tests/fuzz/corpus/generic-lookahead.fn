fn identity<T>(value T) T { value }
fn typeMarker<T>() void { return }
fn main() i32 {
    const x = 1
    const y = 2
    const p = 5
    const c = 4
    const before = x < y
    const after = p > (c)
    const direct fn(i32) i32 = identity<i32>
    typeMarker<Result<Option<i32>, fn(i32) i32>>()
    if before && after { return direct(42) - 42 } else { return 1 }
}
