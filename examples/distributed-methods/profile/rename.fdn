package example.profile

methods User {
    /// Replaces the name shown by the application.
    fn Rename(
        edit,
        /// The new user-facing name.
        name String
    ) void {
        self.Name = name
    }
}
