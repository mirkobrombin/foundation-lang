package example.profile

methods User {
    // Creates a profile with a display name and starting score.
    ctor New($name String, initialScore i32) {
        User { Name = name Score = initialScore }
    }
}
