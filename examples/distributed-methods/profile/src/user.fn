package example.profile

// A user profile assembled with methods from several source files.
struct User implements Profile {
    // The name shown to people.
    Name String

    // The current profile score.
    Score i32
}
