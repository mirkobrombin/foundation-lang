package example.profile

// Operations supported by a mutable user profile.
contract Profile {
    fn Display(self) void
    fn Rename(&self, $name String) void
    fn AddScore(&self, amount i32) void
}
