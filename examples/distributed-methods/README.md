# Distributed methods example

This project keeps one `User` type and its methods in separate files within the owning package.
It is the executable fixture for method aggregation, documentation hover, signature help,
completion, navigation, and the Composite Type View.

```sh
foundationc run examples/distributed-methods
```
