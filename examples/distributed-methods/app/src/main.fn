package example.app

import example.profile

// Runs the distributed-method example.
fn main() i32 {
    var user = profile.User.New("Ada", 40)
    user.Rename("Ada Lovelace")
    user.AddScore(2)
    user.Display()
    user.Score - 42
}
